# CMake C++ Quick Template

- Use cmake to build
- From build/ directory run `cmake ..` this will set up the cmake project.
- To build `cd build` then run make commands such as  `make clean` and `make`
- To run `./build/<project_name>`
